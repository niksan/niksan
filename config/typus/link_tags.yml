# Use the README file as a reference to customize settings.

LinkTag:
  fields:
    default: tag, tagged_id, tagged_type
    form: tag, tagged_id, tagged_type
  relationships: 
  application: Application
